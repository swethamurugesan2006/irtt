version: 0.1
component: build

steps:
  - type: Command
    name: Build Step
    command: |
      mkdir -p output
      echo "Hello" > output/test.txt
      ls -l output

outputArtifacts:
  - name: output
    type: BINARY
    location: output
