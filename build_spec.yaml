version: 0.1

component: build

timeoutInSeconds: 600

shell: bash

steps:
  - type: Command
    name: Build Step
    command: |
      mkdir -p output
      echo "Hello" > output/test.txt

outputArtifacts:
  - name: output
    type: BINARY
    location: output
